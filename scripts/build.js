// scripts/build.js
// Only handles message logging for now, as manifest points to dist directly
import fs from 'fs';
import path from 'path';

console.log('Building BarryGuard Extension...');
console.log('Build complete. Manifest points to dist/ assets.');
